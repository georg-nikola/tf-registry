apiVersion: v1
kind: Service
metadata:
  name: {{ include "tf-registry.fullname" . }}
  labels:
    {{- include "tf-registry.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "tf-registry.selectorLabels" . | nindent 4 }}
